*.iml
.gradle
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# JVM crash logs
hs_err_pid*.log
replay_pid*.log

# Temp & backup files
*.tmp
*~
*.zip
*.pdf

# Kotlin error logs
.kotlin/

# APK builds (only ignore build output dirs, keep release APKs)
app/build/
submission/android/app/build/

# Keystore files (never commit)
*.keystore
*.jks

# Go backend
backend/.env
submission/.env
deploy/.env
_credentials.md
backend/*.exe
backend/tmp/
deploy/**/*.exe

# Claude Code local settings
.claude/

# Submission build artifacts
submission/android/app/build/
submission/android/.gradle/
submission/android/build/
submission/android/local.properties
backend/server
strip_conflicts.py

# APK 构建产物（>100MB，已超出 GitHub 单文件限制，通过 Releases 分发）
*.apk
apk/

# ONNX 模型文件（从 YOLO 官方源下载）
*.onnx
bfg.jar
